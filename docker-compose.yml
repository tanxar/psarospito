services:
  db:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_USER: app
      POSTGRES_PASSWORD: app
      POSTGRES_DB: nestio
    ports:
      - "5432:5432"
    volumes:
      - nestio_pgdata:/var/lib/postgresql/data

volumes:
  nestio_pgdata:

